using System;

int a = Convert. ToInt32(Console.ReadLine());
int b = Convert. ToInt32(Console. ReadLine());
if(a > b)
{
    Console.WriteLine("max = " + a.ToString());
}
else if (b > a)
{
    Console.WriteLine("max = " + b.ToString());
}